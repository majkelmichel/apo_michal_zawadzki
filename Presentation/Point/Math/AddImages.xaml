<Window x:Class="Presentation.Point.Math.AddImages"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Presentation.Point.Math"
        mc:Ignorable="d"
        Title="AddImages" Height="450" Width="800" d:DataContext="{d:DesignInstance local:AddImages}">
    <StackPanel>
        <Grid Background="LightGray" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Window Name" FontWeight="Bold" />
            <TextBlock Grid.Column="1" Text="Use" FontWeight="Bold" />
        </Grid>
        <ItemsControl ItemsSource="{Binding Windows}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Title}"></TextBlock>
                        <CheckBox Grid.Column="1" Checked="CheckboxChecked"></CheckBox>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <CheckBox Content="Saturate" Click="SaturateChecked"></CheckBox>
        <Button Name="SubmitButton" 
                Content="Submit" 
                Width="100" 
                Height="30"
                Click="SubmitButton_Click"/>
    </StackPanel>
</Window>