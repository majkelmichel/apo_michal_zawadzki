<Window x:Class="Presentation.Analyze.LookUpTableGrayscale"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Presentation.Analyze"
        mc:Ignorable="d"
        Title="LookUpTableGrayscale" Height="450" Width="200" d:DataContext="{d:DesignInstance local:LookUpTableGrayscale}">
        <ScrollViewer>
                <StackPanel>
                        <Grid Background="LightGray" Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"></ColumnDefinition>
                                        <ColumnDefinition Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Index" FontWeight="Bold" />
                                <TextBlock Grid.Column="1" Text="Intensity" FontWeight="Bold" />
                        </Grid>
                        <ItemsControl x:Name="LutItemsControl" ItemsSource="{Binding DisplayRows}">
                                <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                                <Grid>
                                                        <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"></ColumnDefinition>
                                                                <ColumnDefinition Width="*"></ColumnDefinition>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{Binding Index}"></TextBlock>
                                                        <TextBlock Grid.Column="1" Text="{Binding Intensity}"></TextBlock>
                                                </Grid>
                                        </DataTemplate>
                                </ItemsControl.ItemTemplate>
                        </ItemsControl>
                </StackPanel>
        </ScrollViewer>
</Window>
