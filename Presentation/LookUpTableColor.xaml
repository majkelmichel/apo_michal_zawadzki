<Window x:Class="Presentation.LookUpTableColor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Presentation"
        mc:Ignorable="d"
        Title="LookUpTableColor" Height="450" Width="400" d:DataContext="{d:DesignInstance local:LookUpTableColor}">
    <ScrollViewer>
        <StackPanel>
            <Grid Background="LightGray" Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Index" FontWeight="Bold" />
                <TextBlock Grid.Column="1" Text="Red" FontWeight="Bold" />
                <TextBlock Grid.Column="2" Text="Green" FontWeight="Bold" />
                <TextBlock Grid.Column="3" Text="Blue" FontWeight="Bold" />
            </Grid>
            <ItemsControl x:Name="LutItemsControl" ItemsSource="{Binding DisplayRows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Index}" />
                            <TextBlock Grid.Column="1" Text="{Binding Red}" />
                            <TextBlock Grid.Column="2" Text="{Binding Green}" />
                            <TextBlock Grid.Column="3" Text="{Binding Blue}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Window>